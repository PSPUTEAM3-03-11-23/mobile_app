import 'package:flutter/material.dart';

class DataBaseAddView extends StatelessWidget {
  const DataBaseAddView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
